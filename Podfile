# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwipeMatch' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwipeMatch
  pod 'Firebase/Firestore’
  pod 'Firebase/Auth’
  pod 'Firebase/Storage’
  pod 'SDWebImage'
  pod 'JGProgressHUD'

end
